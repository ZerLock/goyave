export interface GlobalConfig {
    start_cards_number: number; // Number of cards distibuted at each players when the game start
    turn_timeout: number; // Time for a player to play at his turn (in SECONDS)
}
